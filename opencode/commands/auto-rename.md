---
description: Suggest a new title for this session and rename it via the local server API
model: opencode/mimo-v2.5-free
subtask: true
---
Read the following session context, then follow the rename-session skill workflow to rename the CURRENT session.

Current session info (sessionID<TAB>current title<TAB>first user message):
!`bash /home/sidat/.dotfiles/opencode/skills/rename-session/scripts/get_current_session.sh "$PWD"`
